version: "3"

services:
  centrifugo:
    build:
      context: .
    volumes:
      - ./config.json:/centrifugo/config.json
    command: centrifugo -c config.json
    ports:
      - 8888:8888
    ulimits:
      nofile:
        soft: 65535
        hard: 65535